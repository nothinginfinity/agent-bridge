name: Deploy afo-demo-template

on:
  push:
    branches: [main]
    paths:
      - 'workers/afo-demo-template/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy afo-demo-template
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Deploy
        run: npx wrangler deploy
        working-directory: workers/afo-demo-template
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
