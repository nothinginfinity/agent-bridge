name: Deploy afo-worker-debundler-mcp

on:
  push:
    branches: [main]
    paths:
      - 'workers/afo-worker-debundler-mcp/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Deploy
        run: npx wrangler deploy
        working-directory: workers/afo-worker-debundler-mcp
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
